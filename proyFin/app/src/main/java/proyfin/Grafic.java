package proyfin;
import javax.swing.JFrame;

public class Grafic extends JFrame {
    setTitle("Recogida de datos");

    setSize(500, 300);

    setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

    setVisible(true);

    setLocation(200, 150);
}
